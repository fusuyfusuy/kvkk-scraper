{
  "name": "kvkk-scraper-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "lint": "bun run --filter '*' lint",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/node": "^22.7.0",
    "vitest": "^2.1.1",
    "eslint": "^9.11.0",
    "prisma": "^5.20.0"
  }
}
